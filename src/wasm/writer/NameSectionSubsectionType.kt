package wasm.writer

enum class NameSectionSubsectionType {
    MODULE,
    FUNCTION,
    LOCAL,
    LABEL,
    TYPE,
    TABLE,
    MEMORY,
    GLOBAL,
    ELEM_SEGMENT,
    DATA_SEGMENT,
    FIELD,
    TAG
}